# =============================================================================
# ShipYard Docker Configuration
# =============================================================================
# Copy this file to .env and fill in the values before running docker-compose
# Or run ./install.sh to set up automatically
# =============================================================================

# HTTP/HTTPS Ports
HTTP_PORT=80
HTTPS_PORT=443

# Database Configuration (required - no defaults for security)
DB_DATABASE=server_management
DB_USERNAME=
DB_PASSWORD=
DB_ROOT_PASSWORD=
